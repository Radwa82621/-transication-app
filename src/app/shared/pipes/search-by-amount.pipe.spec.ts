import { SearchByAmountPipe } from './search-by-amount.pipe';

describe('SearchByAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
